sync_now:
  name: Sync now
  description: Immediately fetch fixtures and create any missing calendar events.
  fields:
    entry_id:
      name: Entry ID
      description: Optional config entry id. If omitted, all Fixture Sync entries run.
      required: false
      selector:
        text:
